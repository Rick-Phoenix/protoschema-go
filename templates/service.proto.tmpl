{{ define "service" }}

{{ range .Services }}
service {{ serviceSuffix .Resource }} {
{{ range .Options -}}
  {{ fmtOpt . }};
{{ end }}
  {{- range .Handlers}}
  rpc {{ .Name }}({{ .Request.Name }}) returns({{  .Response.Name  }});
  {{- end}}
}
{{ end }}

{{ end }}
